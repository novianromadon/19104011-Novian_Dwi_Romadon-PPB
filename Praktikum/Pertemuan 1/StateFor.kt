fun StateFor () {
    val ranges = 1.rangeTo(10) step 3
    
    //i => variabel yang dipake di dalam loop
    // ranges => array atau jumlah batasnya
  
    for (i in ranges ){
      println("value is $i!")
    }
  }